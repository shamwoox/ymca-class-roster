<% include ../partials/header %>

<div class="container">
    <div class="parent">
        <div class="form" style="text-align: center">
            <div class="row">
                <div class="shadow-lg p-3 mb-5 bg-white rounded">
                    <h2>Add New Day</h2>
                    <hr>
                    <form action="/session/<%= foundClass.session.id %>/class/<%= foundClass._id %>/day" method="POST">
                        <h6>Enter Name of Instructor:</h6>
                        <input class="form-control" type="text" name="instructor" value="<%= foundClass.instructor.firstName %> <%= foundClass.instructor.lastName %>">
                        <!-- <hr>
                        <h6>Add Skills To Be Taught</h6>
                        <% skills.forEach(function(skill) { %>
                            <div class="row">
                                <div class="container">
                                    <div class="form-check float-left">
                                        <input class="form-check-input" type="checkbox" value="<%= skills %>" id="skills">
                                        <label class="form-check-label" for="skills">
                                            <%= skill %>
                                        </label>
                                    </div>
                                </div>
                            </div>
                        <% }) %> -->
                        <hr>
                        <button class="btn btn-primary btn-sm float-left" type="submit">Submit</button>
                        <a class="btn btn-danger btn-sm float-right" href="/session/<%= foundClass.session.id %>/class/<%= foundClass._id %>">Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<% include ../partials/footer %>