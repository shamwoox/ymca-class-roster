<% include ../partials/header %>

<div class="container">
  <div class="parent">
    <div class="form" style="text-align: center;">
      <div class="row">
        <div class="shadow-lg p-3 mb-5 bg-white rounded">
					<form action="/session" method="POST">
						<h2>New Session</h2>
						<hr>
						<div class="form-row">
							<div class="form-group col-md-6">
								<label for="sessionSeason">Session Season</label>
								<select name="session[season]" id="sessionSeason" class="form-control">
									<option disabled selected>Select...</option>
									<% seasons.forEach(function(season) { %>
										<option value="<%= season %>"> <%= season %> </option>
									<% }) %>
								</select>
							</div>
							<div class="form-group col-md-6">
								<label for="sessionYear">Session Year</label>
								<select name="session[year]" id="sessionYear" class="form-control">
									<option disabled selected>Select...</option>
									<% var year =  (new Date()).getFullYear() %>
									<% for(var i = year, n = year + 3; i < n ; i++) { %>
										<option value="<%= i %>"><%= i %></option>
									<% } %>
								</select>
							</div>
						</div>
						<div class="form-row">
							<div class="form-group col-md-6">
								<label for="sessionStart">Start Date</label>
								<!-- <input type="text" name="session[startDate]" id="sessionStart" class="form-control"> -->
								<input id="date" name="session[startDate]" class="form-control" type="date">
							</div>
							<div class="form-group col-md-6">
								<label for="sessionEnd">End Date</label>
								<!-- <input type="text" name="session[endDate]" id="sessionEnd" class="form-control"> -->
								<input id="date" name="session[endDate]" class="form-control" type="date">
							</div>
						</div>
						<hr>
						<button type="submit" class="btn btn-primary btn-sm">Submit</button>
						<a href="/session" class="btn btn-danger btn-sm">Cancel</a>
					</form>
      	</div>
			</div>
		</div>
	</div>
</div>

<% include ../partials/footer %>